import { _makeConversionAjaxCall } from './actions';
import "babel-polyfill";
import { exportAllDeclaration } from '@babel/types';

describe("Make conversion ajax call", () => {
    it("should make conversion ajax call", async () => {
        console.log("this is tested");
    });
});